# Code review, pass 2 — S2: copy-descended subsystems

**Status: decision document — awaiting markup.** Produced by the pass-2 catalog run
(24 finders/planners + adversarial verification of every delete/unexport and
importable/collapsible claim; full structured data in
`docs/proposals/review-pass2.local/catalog.json`). Nothing here has been changed yet;
mark the response blocks and the approved items become the implementation plan.

Three consolidation designs, each derived from a precise diff of the twins.

---

## The room-relay backends (pencil + remote-bar)

**Twins:** packages/aiui-pencil/src/backend.ts (375 lines) + packages/aiui-pencil/src/sidecar.ts (93 lines) · packages/aiui-remote-bar/src/backend.ts (376 lines) + packages/aiui-remote-bar/src/sidecar.ts (68 lines)

**Diff summary:** BACKENDS — line-for-line identical (modulo names/comments) for ~300 of ~375 lines; every divergence is message-vocabulary, none is room mechanics. Identical, with (pencil:line / bar:line): ChannelResolver type, textually identical (47/53); HEARTBEAT_MS=30_000 (50/56); options shape prefix/session/resolveChannel/log (52-61/58-74); Backend interface handleHttp/handleUpgrade/sessions/counts/dispose (63-74/76-87); ClientConn (87-91/100-104); parseRequestUrl never-throw (95-101/108-114); send-if-OPEN (104-108/117-121); sessions()+broadcastSessions (118-126/131-139); heartbeat WeakMap/track/terminate/unref block (129-152/142-166); two noServer WebSocketServers (155-156/169-170); host-connection seed with `host-${++seq}` id, session-identity spread, `registered` frame (158-177/174-193); register base handling — label||"app", project, channelPort→resolveChannel with ??-preserving merge, registered=true, log, broadcastSessions (187-206/203-221); host close → hostGone fan-out + broadcast (225-233/232-240); client seed + initial sessions frame (237-241/245-249); leaveRoom with busy recompute (243-253/251-261); join flow — leaveRoom, lookup, joinRejected "host not found", busy=true, joined → cached-replay → clientJoined → broadcastSessions ordering (263-288/271-289); leave + !conn.host guard (289-295/291-297); client close (307-311/304-308); sendJson with CORS (315-320/312-317); handleHttp GET-only /info|/health→{ok,...counts}, /sessions (322-339/319-340); handleUpgrade /host,/client (341-355/342-356); counts/dispose (357-372/358-373); binary-frame drop on both socket kinds (179-181,255-257 / 195-197,263-265). DIVERGED (why: different payload domains): (1) replay-cache field: lastVideoStatus:VideoStatus (pencil 82,164,210,281) vs lastBar:BarState (bar 95,180,225,284) — same cache-on-publish + replay-on-join semantics, different frame; (2) register extras: pencil accepts `presentation` from the wire (192-194, protocol.ts:239) but NOT channelTag; bar accepts `channelTag` (208-210, protocol.ts:144) but no presentation — SessionInfo differs only by presentation? (pencil protocol.ts:213 vs bar protocol.ts:112-125); (3) pencil-only WebRTC signaling: host→one-client routing by message.peer, dropped when peer undefined (216-222), and client→host stamping {...message, peer: conn.id} (300-304) — bar has no analogue; (4) client→host forward predicate: isInkIntent (296) vs isRemoteCommand (299); (5) joined frame: pencil spreads presentation (273-278) vs bare {type,host,label} (bar 281); (6) log prefix "pencil:" vs "bar:" (203/219). Room wire frames (registered/sessions/join/joined/joinRejected/leave/clientJoined/clientLeft/hostGone/register-base) are structurally identical across both protocol.ts unions (pencil 220-251, bar 129-163). SIDECARS — bar's sidecar is a strict subset of pencil's: identical mount idiom (backend-first passthrough middleware `if (!backend.handleHttp(req,res)) next()` — pencil sidecar.ts:58-62, bar sidecar.ts:53-57), handleUpgrade delegation, dispose. Pencil adds the iPad page via aiui-util's serveClientSurface (sidecar.ts:68-77), making mount async and composing surface+backend upgrade/dispose (83-89); that page-serving half is ALREADY factored into @habemus-papadum/aiui-util/web-surface, so beyond the 6-line middleware idiom the sidecars have little left to share. Note: resolveChannel is unwired in both production sidecars (neither passes it; bar protocol.ts:146-152 documents this) — only options.session flows in (pencil sidecar.ts:52, bar sidecar.ts:49).

**Shared core:** home: Primary recommendation: a NEW published leaf package `@habemus-papadum/aiui-room-relay` (--public, runtime dep: `ws` only; devDeps for express/@types like the twins). Both twins are published public, so the core must be public too (a --private core would break installs; --no-publish would break them outright). Runner-up: an `./room-relay` subpath of @habemus-papadum/aiui-util (already published+trusted, subpath pattern proven by ./web-surface, pencil already depends on it at runtime — pencil package.json:76) — REJECTED as primary because aiui-util's runtime deps are `@puppeteer/browsers` + `execa` (aiui-util/package.json:46-49): aiui-remote-bar consumers would newly install that heavyweight pair for ~330 lines of relay. Fallback if npm ceremony blocks (MEMORY: npm:trust pending 2FA, paid-plan E402): VENDORING — canonical source in one private workspace location, a copy step stamps a committed `src/room-relay.generated.ts` into each twin plus a CI regen-and-diff drift guard; zero new npm names, zero dep changes, published artifacts self-contained. aiui-build-config is NOT a candidate (build config only, per constraints). New package via `pnpm new-package aiui-room-relay --public` gets the source-first/publishConfig shape from the skeleton; requires pnpm npm:reserve + npm:trust before the next release.

`createRoomRelayBackend<M extends {type:string}>(options: RoomRelayOptions<M>): RoomRelayBackend` where RoomRelayBackend is exactly today's five-method seam {handleHttp, handleUpgrade, sessions, counts, dispose} (pencil backend.ts:63-74). Options = today's {prefix?, session?, resolveChannel?: ChannelResolver, log?} (ChannelResolver moves to the core and is re-exported by both twins) PLUS a vocabulary delegate: { logPrefix: string; decode(text): M|undefined; encode(m: M): string; registerExtras?(msg): Partial<SessionInfo> — pencil returns {presentation}, bar returns {channelTag}, preserving the asymmetry that pencil ignores wire channelTag; joinedExtras?(info: SessionInfo): object — pencil returns {presentation}, bar returns {}; onHostMessage(msg, ctx: {cacheForReplay(m): void /* set replay slot + fan out to room */, sendToClient(id, m): boolean /* pencil signal→peer, drops undefined peer */}): void; onClientMessage(msg, ctx: {clientId, sendToHost(m): void}): void /* pencil: ink forward + signal stamped {…m, peer: clientId}; bar: isRemoteCommand forward */ }. Core owns generically: ids/registries, heartbeat+unref, register base (label/project/channelPort→resolveChannel merge), sessions/broadcast, join/leave/joinRejected/clientJoined/clientLeft/hostGone/busy, replay slot + the exact joined→replay→clientJoined→broadcast ordering, binary-frame drop, parseRequestUrl/send/sendJson, GET /info|/health|/sessions with CORS, /host|/client upgrade routing, counts, dispose. Core also exports the generic room-frame types; each protocol.ts keeps its authoritative unions (already structurally identical) tied by assignability, so wire shapes cannot change.

**Per-twin residue:** aiui-pencil keeps: protocol.ts untouched (ink/video/signal vocab, RemotePresentation, norm-coordinate math); backend.ts shrinks to ~60 lines — a vocabulary delegate (videoStatus→cacheForReplay; signal→sendToClient(msg.peer); client-side isInkIntent forward + signal peer-stamping; registerExtras/joinedExtras = presentation; logPrefix "pencil") wrapped as `createPencilBackend` with the same exported names/types so sidecar.ts, lab/vite.config.ts:30, and backend.test.ts (242 lines) compile unchanged; sidecar.ts keeps serveClientSurface page-serving wholesale. aiui-remote-bar keeps: protocol.ts untouched (BarState/RemoteCommand, its register channelTag field); backend.ts shrinks to ~40 lines (bar→cacheForReplay; isRemoteCommand→sendToHost; registerExtras = channelTag; logPrefix "bar") as `createBarBackend`; sidecar.ts unchanged. Both twins keep their README route docs and their full test suites as the behavior-preservation oracle — existing backend.test.ts files (242 + 314 lines) must pass unmodified against the wrappers.

**Risks:** 1) Wire compatibility: both twins are published; old iPad clients may hit a new relay. Frames are unchanged by design, but the extraction invites 'normalizing' the register asymmetry (pencil ignoring wire channelTag, bar lacking presentation) — that is a behavior change and must be resisted in this refactor. 2) Packaging: the new subpath/package must carry the dual dev-exports + publishConfig shape with a trailing \"default\" condition, and `pnpm test:packaging` must run — source-first dev masks dist-only export bugs (the PR #1 ERR_PACKAGE_PATH_NOT_EXPORTED class). 3) npm ceremony: a new public name needs pnpm npm:reserve + npm:trust, both currently friction-laden (2FA pending per memory); if blocked, ship the vendoring fallback rather than parking the refactor on the util subpath and dragging @puppeteer/browsers+execa into remote-bar installs. 4) Generic typing over the two message unions (Extract on \"register\", replay-frame type) can get fiddly; acceptable fallback is a loosely-typed core interior with strongly-typed wrapper boundaries. 5) Heartbeat semantics (terminate on missed pong, setInterval unref) must carry over verbatim or test processes hang / zombie viewers hold rooms busy. 6) Version lockstep means the first post-extraction release ships pencil, remote-bar, and the new core together — fine, but the core must be reserved/trusted BEFORE that release or release.yml fails partway. 7) Node-only `ws` in the core: keep it out of any browser-reachable export path (same discipline as memory note on ws/jsdom Vitest splits; remote-bar already runs split test projects).

**Effort:** Small-to-medium: core is ~330 lines extracted nearly verbatim plus two ~40-60-line delegates; existing 556 lines of backend tests are the acceptance oracle and should pass unmodified. Roughly half a day of code + packaging (new-package scaffold, exports/publishConfig, test:packaging, typecheck across the workspace), plus the one-time npm reserve/trust ceremony if the new-package home is chosen. Vendoring fallback is similar code effort plus a ~50-line copy/drift-guard script.

- [ ] Approve as proposed
- [ ] Partially (see comments)
- [ ] Defer
- [ ] Drop

Comments:

---

## The voice-vendor sessions (channel)

**Twins:** packages/aiui-claude-channel/src/realtime.ts (OpenAI realtime STT; RealtimeSession seam, 743 lines) · packages/aiui-claude-channel/src/elevenlabs-realtime.ts (Scribe v2 STT; same RealtimeSession seam, 797 lines) · packages/aiui-claude-channel/src/openai-live.ts (OpenAI conversational linter; LiveSession seam, 418 lines) · packages/aiui-claude-channel/src/gemini-live.ts (Gemini Live linter; same LiveSession seam, 466 lines) · packages/aiui-claude-channel/src/realtime.test.ts:33-70 (FakeUpstream #1: +raw()) · packages/aiui-claude-channel/src/openai-live.test.ts:7-36 (FakeUpstream #2: minimal) · packages/aiui-claude-channel/src/gemini-live.test.ts:7-36 (FakeUpstream #3: minimal, verbatim twin of #2) · packages/aiui-claude-channel/src/elevenlabs-realtime.test.ts:26-72 (FakeUpstream #4: superset — +url/apiKey capture, +raw(), +serverClose(code,reason))

**Diff summary:** IDENTICAL (verbatim or near-verbatim across all four sessions): (1) the sendReady queue-until-ready gate — `if (ready && !dead) socket.send(text); else if (!dead) outbox.push(text)` — realtime.ts:444-451 (as sendAudioMessage), openai-live.ts:139-146, gemini-live.ts:218-225, elevenlabs-realtime.ts:495-503 (only delta: one `armKeepalive()` call after a real send); (2) the ready-signal flush loop `for (const queued of outbox.splice(0)) socket.send(queued)` — realtime.ts:540-542, openai-live.ts:226-228, gemini-live.ts:285-287, elevenlabs-realtime.ts:624-626; (3) the JSON.parse-or-silently-return handleMessage prologue in all four (realtime.ts:515-519, openai-live.ts:218-222, gemini-live.ts:277-282, elevenlabs-realtime.ts:599-603); (4) close() = `dead = true; try socket.close() catch {}` — openai-live.ts:409-416 ≡ gemini-live.ts:438-445; realtime.ts:730-742 and elevenlabs-realtime.ts:783-795 add releasing drainWaiters (elevenlabs also clearKeepalive); (5) onClose→fail with closeSuffix — realtime.ts:660-667, openai-live.ts:333-342, gemini-live.ts:372-384, elevenlabs-realtime.ts:691-699; the live pair also builds an identical `{closeCode, closeReason}` data object. IDENTICAL within the STT pair (realtime ↔ elevenlabs): drainWaiters + settleDrainIfIdle (realtime.ts:432-440 ≈ elevenlabs:443-450), drain(timeoutMs) with settled/finish/timer (realtime.ts:712-729 ≈ elevenlabs:765-781 — only the outstanding-snapshot expression differs: `[...pending]` vs `committed.map(c=>c.segment)`), loud multi-segment fail (realtime.ts:497-509 ≈ elevenlabs:668-683: splice outstanding, per-segment onError, session-wide onError if none, settle), dead-commit guard `onError("realtime session unavailable", segment)` (realtime.ts:682-684 ≡ elevenlabs:721-723), streamingSegment set-on-append / clear-on-commit-or-discard (realtime.ts:678,686-688,697-699 ≈ elevenlabs:717,725-727,759-761), and the RealtimeDiagnostic vocabulary (config-echo/config-mismatch/vendor-commit/orphan-result/unhandled, realtime.ts:105-110) consumed by both. IDENTICAL within the live pair (openai-live ↔ gemini-live): simple fail (openai-live.ts:148-154 ≡ gemini-live.ts:247-253), reply-clip flush shape (buffer PCM chunks → pcm16ToWav(concatChunks(...)) → onReplyAudio; accumulate transcript → trim → onReplyTranscript: openai-live.ts:156-168 ≈ gemini-live.ts:234-244), and buildLinterCall's once-guarded `{tool, args, respond}` (openai-live.ts:171-207 ≈ gemini-live.ts:256-274 — only the respond wire body differs). BYTE HELPERS, 4x copy-pasted: `toBase64` at realtime.ts:378 (misnamed textDecoderBase64), openai-live.ts:93, gemini-live.ts:149, elevenlabs-realtime.ts:245; `fromBase64` at openai-live.ts:94 ≡ gemini-live.ts:150; `concatChunks` VERBATIM at openai-live.ts:96-108 ≡ gemini-live.ts:153-165. pcm.ts (42 lines) already exists as the shared PCM home and is imported by both live sessions. WS SOCKET FACTORIES, 3x near-verbatim: realtime.ts:385-396, gemini-live.ts:172-187, elevenlabs-realtime.ts:253-264 — identical `ws` event wiring + captureUnexpectedResponse; the ONLY difference is auth placement (Bearer header / `?key=` query param / `xi-api-key` header). closeSuffix (realtime.ts:257-266) and captureUnexpectedResponse (realtime.ts:295-328) are ALREADY shared — realtime.ts is the accidental commons. TEST FIXTURES: four copy-pasted FakeUpstream closures; #2 and #3 are verbatim twins; #1 adds raw(); #4 (elevenlabs) is a strict superset of all three (url/apiKey capture, raw(), serverClose(code,reason)) — every existing test compiles unchanged against the superset. DIVERGED, AND WHY (must stay per-twin): segment↔result correlation — OpenAI's wire carries item_id so realtime.ts binds via segmentForItem + awaitingItem/boundSegments/discardedItems (realtime.ts:460-481, 417-431); Scribe's wire carries NO ids so elevenlabs correlates positionally via a FIFO of CommittedSegment plus vendor-self-commit accumulation in completeHead (elevenlabs:530-577) — different algorithms forced by wire facts, documented by live post-mortems. discard: OpenAI has input_audio_buffer.clear (realtime.ts:696-711); Scribe has no clear message and committing scrap is fatal, so discard sends nothing (elevenlabs:753-764). Commit floors: openai-live 100 ms → clear (openai-live.ts:72-74, 364-368); elevenlabs 500 ms → empty final with NO wire frame (elevenlabs:179, 737-748); realtime.ts has none. Keepalive: elevenlabs only (~15 s idle close; elevenlabs:462-490). Window ordering: gemini only (WindowOrderingGuard, gemini-live.ts:101-132, the 1007 audio-first rule). Reply keying: response_id Map (openai-live.ts:136-137) vs single turnComplete-bounded buffer + interrupted reset (gemini-live.ts:213-215, 308-316). Tool resume: explicit response.create after function_call_output (openai-live.ts:193-204) vs automatic after toolResponse (gemini-live.ts:266-272). Readiness signal + config transport: session.update→session.updated (realtime, openai-live) vs setup→setupComplete (gemini) vs URL-is-the-config→session_started (elevenlabs, buildElevenLabsUrl elevenlabs:274-303). Config-echo checkers differ in param vocabulary (checkRealtimeConfigEcho realtime.ts:121-144 vs checkConfigEcho elevenlabs:368-388) and stay vendor-specific.

**Shared core:** home: All intra-package — no new workspace edges, aiui-build-config not involved. (a) packages/aiui-claude-channel/src/session-core.ts (new, internal, NOT exported from index.ts): ready-gate, drain controller, loud-fail reporter, ws-factory maker, reply-clip accumulator, once-guarded tool-call builder. (b) packages/aiui-claude-channel/src/pcm.ts (exists): absorb toBase64/fromBase64/concatChunks — it is already 'PCM/WAV helpers shared by the realtime engines' per its own header. (c) packages/aiui-claude-channel/src/fake-upstream.ts (new shared test fixture): the elevenlabs superset FakeUpstream; either extend tsconfig.json's build exclude (currently only src/**/*.test.ts, tsconfig.json:9) with the fixture path, or accept a stray d.ts in dist (vite build bundles from entries so no runtime JS ships).

session-core.ts: (1) createReadyGate(rawSend: (text: string) => void, opts?: { onSent?: () => void }): { send(text: string): void; markReady(): void; markDead(): void; isDead(): boolean } — send queues until markReady flushes; onSent fires only on REAL sends (preserves elevenlabs armKeepalive at elevenlabs:499); rawSend is a lazy lambda `(t) => socket.send(t)` so the socket-defined-after-handlers circularity survives. (2) createDrainController(outstanding: () => number[]): { settleIfIdle(): void; drain(timeoutMs: number): Promise<number[]>; releaseAll(): void } — outstanding() is snapshotted at RESOLUTION time (unifies `[...pending]` and `committed.map(...)`); settleIfIdle resolves waiters when outstanding() is empty; releaseAll backs close(). (3) reportSessionFailure(onError: RealtimeCallbacks['onError'], message: string, segments: number[]): void — per-segment loudly, session-wide when empty (the shared tail of both STT fails; each twin still does its own state teardown first). (4) makeWsSocketFactory(auth: (url: string, apiKey: string) => { url: string; headers?: Record<string, string> }): RealtimeSocketFactory — one `ws` wiring (open/message/error/close-with-code-reason + captureUnexpectedResponse); the three vendor factories become one-line auth lambdas. closeSuffix + captureUnexpectedResponse + RealtimeSocketHandlers/RealtimeSocket/RealtimeSocketFactory + RealtimeDiagnostic migrate here from realtime.ts WITH re-exports left in realtime.ts (elevenlabs/gemini/openai-live all import them from './realtime' today — realtime.ts stops being the accidental commons without breaking importers). (5) createReplyClip(cb: { onAudio(wav: Uint8Array, mime: string): void; onTranscript(text: string): void }, rate: number): { pushAudio(bytes: Uint8Array): void; appendTranscript(t: string): void; setTranscript(t: string): void; flush(): void; reset(): void } — gemini holds one instance (reset on interrupted), openai-live a Map<responseId, instance>. (6) makeOnceCall(name: string, args: Record<string, unknown>, isDead: () => boolean, sendResponse: (result: string) => void): LinterToolCall — the responded/dead once-guard; the vendor respond body is the lambda. pcm.ts additions: export const toBase64(bytes: Uint8Array): string; export const fromBase64(b64: string): Uint8Array; export function concatChunks(chunks: Uint8Array[]): Uint8Array. fake-upstream.ts: export interface FakeUpstream { factory: RealtimeSocketFactory; url?: string; apiKey?: string; sent: Array<Record<string, unknown>>; closed: boolean; open(): void; emit(message: Record<string, unknown>): void; raw(text: string): void; error(message: string, data?: unknown): void; serverClose(code?: number, reason?: string): void }; export function fakeUpstream(): FakeUpstream — the elevenlabs-realtime.test.ts:26-72 shape verbatim; the other three fixtures are strict subsets so all existing tests port by deleting their local copy and importing.

**Per-twin residue:** realtime.ts keeps: the OpenAI transcription event vocabulary and its switch (delta/completed/failed/error, realtime.ts:511-624), item_id binding (segmentForItem + pending/awaitingItem/boundSegments/discardedItems/commitAt/itemToSegment/cumulativeByItem, realtime.ts:415-494), accumulate-before-bind delta ordering (realtime.ts:548-551), wordsFromTokenLogprobs (210-255), checkRealtimeConfigEcho (121-144), the session.update config frame + gpt-realtime-whisper-only delay knob (626-657), wire discard via input_audio_buffer.clear. elevenlabs-realtime.ts keeps: completeHead FIFO + vendor-self-commit accumulation incl. the uncommittedBytes-reset-on-self-commit rule (530-577, esp. 554), the 500 ms commit floor with empty-final resolution (737-748), byte meters + timestamp rebasing (convertWords, segmentBaseMs, 314-342, 434-438), keepalive machinery (462-490), buildElevenLabsUrl + checkConfigEcho + ELEVENLABS_ERROR_TYPES/isErrorType (206-303, 359-388), no-op discard. openai-live.ts keeps: its event switch (response.output_audio.delta/.done, response.done usage+function_call extraction, the response_cancel_not_active suppression, 209-296), the 100 ms floor → clear (364-368), turn-item image injection + silent-context items (373-402), the explicit resume rule (response.create after function_call_output), session.update linter config (299-330). gemini-live.ts keeps: WindowOrderingGuard + the emit() routing (101-132, 228-232), the serverContent switch (turnComplete/interrupted/inputTranscription, 276-345), parseTimeLeftMs + goAway (337-345, 454-466), clientContent incremental context (418-433), toolResponse auto-resume, the setup frame (347-369). Wire vocabulary, event parsing, and every documented live-verified quirk stay vendor-local by design.

**Risks:** These four files are live-verified protocol state machines with post-mortem freight (the Scribe self-commit at elevenlabs-realtime.ts:63-96 destroyed user transcripts for months); the refactor must be strictly under the existing ~2400 lines of tests, which are strong on exactly the fragile orderings. Specific traps: (1) createReadyGate's onSent hook must fire only when a frame actually hits the socket — elevenlabs arms keepalive in the ready branch only (elevenlabs:499); firing it on queued frames would re-arm the timer during pre-ready buffering. (2) The ready-flush bypasses the gate (raw socket.send after `ready = true`) in all four — markReady must flush in enqueue order before any new send interleaves. (3) DrainController must snapshot outstanding() at resolution time, not at drain() call — realtime.ts:724 resolves `[...pending]` as it stands when the timer/settle fires. (4) The loud-fail extraction must not absorb per-twin teardown: elevenlabs' fail also clears keepalive and pending (668-683) and realtime's clears commitAt per segment (499-504) — only the onError fan-out is shared. (5) Moving RealtimeSocket* types/closeSuffix/captureUnexpectedResponse out of realtime.ts breaks three importers unless realtime.ts re-exports them (behavior-preserving requires keeping those import paths or a mechanical update in the same change). (6) The shared fixture's close() must keep calling handlers.onClose() with NO args (the closeSuffix("") path several tests assert on); and the fixture file must not match vitest's `*.test.ts` glob (an empty 'test' file fails the run) while ideally staying out of the tsc declaration build (tsconfig.json:9 excludes only src/**/*.test.ts — extend it or accept a stray d.ts). (7) createReplyClip unification is the riskiest of the wins: gemini's interrupted-reset (gemini-live.ts:308-313) and openai's per-response Map keyed by possibly-empty-string ids (openai-live.ts:232-237) have different lifecycle edges — worth doing last or deferring. (8) Do NOT try to unify segmentForItem with completeHead — they are different correlation algorithms forced by different wires; a 'generic binder' abstraction would obscure the exact reasoning the file-header post-mortems exist to preserve. Everything proposed is intra-package, so no publishConfig/exports changes and no packaging-test exposure.

**Effort:** Medium-low. Phase 1 (mechanical, ~half day): byte helpers → pcm.ts, shared FakeUpstream fixture, makeWsSocketFactory, type/helper migration with re-exports — pure deletion of verbatim copies, existing tests unchanged. Phase 2 (careful, ~1 day): createReadyGate + createDrainController + reportSessionFailure threaded through the two STT sessions, then the two live sessions — each file's diff is small but must be reviewed against orderings (1)-(4). Phase 3 (optional): createReplyClip + makeOnceCall for the live pair — smallest win, highest subtlety, safely deferrable. Net deletion estimate: ~250-300 lines of session code + ~120 lines of fixture code removed against ~150 lines of session-core added.

- [ ] Approve as proposed
- [ ] Partially (see comments)
- [ ] Defer
- [ ] Drop

Comments:

---

## Channel commands + repo scripts

**Twins:** packages/aiui-claude-channel/src/commands/mcp.ts (runMcp, 271 ll.) ⇄ packages/aiui-claude-channel/src/commands/serve.ts (runServe, 499 ll.) — the real MCP channel vs the agent-less debug channel: same registry/web/staleness/shutdown lifecycle around different session seams (stdio MCP notifications vs stdout delimited protocol) · packages/aiui-claude-channel/src/program.ts — the `mcp` and `serve` subcommands duplicate the --bind option verbatim including its inline validator (43-53 vs 84-93) and the --mode option text (54-59 vs 94-99, validator already shared via parseMode at 20-25) · scripts/new-package.mjs (207 ll., plain node) ⇄ scripts/new-demo.ts (273 ll., tsx) — two scaffolders sharing fail/slugify/currentVersion/deriveContext/arg-loop in two dialects; new-demo is TS-through-tsx solely because it imports create-aiui's scaffoldApp (new-demo.ts:30, rationale at 11-14; wiring: package.json:28-29)

**Diff summary:** PAIR (a) mcp.ts vs serve.ts — IDENTICAL (modulo the "[aiui-channel]" vs "[aiui-channel serve]" stderr prefix): tag default `options.tag ?? randomUUID()` (mcp.ts:84 / serve.ts:392); sidecar default `options.sidecars ?? standardSidecars(process.cwd())` (mcp.ts:98 / serve.ts:325); channelLog creation from the cache dir (mcp.ts:142 / serve.ts:313); the three conditional startWebServer spreads — mode-if-defined (mcp.ts:177 / serve.ts:351), host:"0.0.0.0" iff bind==="host" (mcp.ts:178 / serve.ts:369), tag-iff-explicit (mcp.ts:182 / serve.ts:354); the AIUI_CHANNEL_WATCH staleness skeleton — env gate, channelSourceDir(), watchChannelSource, the "watching <dir> for staleness" line and the "ignored — not running from source" line (mcp.ts:194-223 / serve.ts:436-454); exit backstop on registry removal (mcp.ts:242-244 process.on with closure / serve.ts:396 process.once with direct ref — behaviorally identical since "exit" fires once); the shutdown shape — idempotence flag, channelLog.log("shutdown"), stopWatch?.(), registration.remove(), await web.close().catch(()=>{}), channelLog.close() last (mcp.ts:228-240 / serve.ts:456-469); "up" stderr line + channelLog.log("up", …) (mcp.ts:256-268 / serve.ts:485-496); and the tag/sidecars/bind/mode option JSDoc, near-verbatim between McpOptions (mcp.ts:14-58) and ServeOptions (serve.ts:81-131). DIVERGED, ALL DELIBERATE: (1) cache dir — mcp pins projectCacheDir() (mcp.ts:138), serve honors options.cacheDir for hermetic tests (serve.ts:309); (2) staleness onChange — mcp keeps a `notified` toggle producing STALE_NOTICE then a "changed again" message, logs "stale", and pushes kind "channel-stale" into the session (mcp.ts:198-213); serve has no session so it narrates one constant stderr line (serve.ts:442-444); (3) shutdown extras — mcp additionally closes the MCP server (mcp.ts:238); serve additionally unsubscribes the page-tools narrator (serve.ts:464) and closes the JSONL recorder (serve.ts:467); error-swallowing differs per closer (mcp catches web+mcp close, serve catches only web.close — recorder.close and channelLog.close are unswallowed in both); (4) signal wiring — mcp: process.on for SIGINT/SIGTERM/SIGHUP plus mcp.onclose→shutdown (mcp.ts:245-253); serve: process.once for SIGINT/SIGTERM only, with a stderr narration line, `once` deliberately so a second Ctrl-C during a wedged shutdown hits the default handler (serve.ts:470-477); (5) registration extras — serve passes {debug:true, name} (serve.ts:392-395), mcp passes none (mcp.ts:184); (6) serve wraps startWebServer in try/catch to close recorder+channelLog and rewrap EADDRINUSE with a pinned-port explanation (serve.ts:372-387) — mcp has no pinned port; (7) everything session-shaped is unshared by design: mcp's pushToSession + page-tools MCP notification ladder (mcp.ts:119-168) vs serve's stdout delimited prompt/page-tools protocol, frame narrator, and trace-stage narration (serve.ts:340-431, 203-297). PAIR (b) new-package.mjs vs new-demo.ts — IDENTICAL: fail() (new-package.mjs:39-42 / new-demo.ts:37-40); slugify() char-for-char (new-package.mjs:87-92 / new-demo.ts:64-69); scriptsDir/repoRoot derivation (new-package.mjs:34-35 / new-demo.ts:32-33); currentVersion — execFileSync node versioning.mjs current, cwd repoRoot, trim (new-package.mjs:134-137 inline / new-demo.ts:81-86); the empty-slug guard (new-package.mjs:125-127 / new-demo.ts:224-226) and the dest-exists refusal (new-package.mjs:129-131 / new-demo.ts:229-231); the parseArgs loop skeleton (positional-once, --description consumes next arg, unexpected-arg fail, missing-name fail: new-package.mjs:55-85 / new-demo.ts:42-62). DIVERGED: deriveContext returns the same {scope, repoUrl} shape but new-package scans packages/* for the first scoped package (new-package.mjs:95-107) while new-demo reads packages/aiui directly (new-demo.ts:72-78, called twice at 241 and 265) — identical output in the current tree since every package shares the scope and repo URL; parseArgs bodies differ materially (mandatory publication level via LEVELS + --no-reserve vs --help/-h exit-0, which new-package lacks); and the entire scaffold payloads differ (skeleton copy + token replace + npm reserve vs scaffoldApp + package.json rewire + CLAUDE.md splice). The dialect split is load-bearing: new-package must stay plain-node (`node scripts/new-package.mjs`, package.json:28) while new-demo must stay tsx (imports create-aiui TS source).

**Shared core:** home: PAIR (a): a new module packages/aiui-claude-channel/src/commands/lifecycle.ts (package-internal, not exported from index — both consumers are in-package; NOT aiui-build-config, which is for build config, and this is runtime behavior). program.ts twins stay in program.ts as a named parseBind beside parseMode plus a small addChannelServerOptions(cmd) applier. PAIR (b): a new plain-ESM scripts/lib/common.mjs with JSDoc types — the only dialect both consumers can import (plain node cannot load .ts; tsx loads .mjs fine). Not a workspace package: scripts must run in a fresh checkout independent of pnpm linking, and none of this is build config, so aiui-build-config is again the wrong home.

lifecycle.ts: (1) `interface CommonChannelOptions { tag?; sidecars?; bind?: "loopback"|"host"; mode?: "dev"|"prod" }` carrying the shared JSDoc; McpOptions/ServeOptions extend it. (2) `resolveSidecars(opts): Sidecar[]` — the `?? standardSidecars(process.cwd())` default. (3) `commonWebOptions(opts)` returning the three conditional spreads ({...mode}, host iff bind==="host", tag iff explicit) to splat into startWebServer. (4) `startStalenessWatch({ logPrefix, onStale }): (() => void) | undefined` — owns the env gate, channelSourceDir(), watchChannelSource, and both stderr lines; the twin-specific reaction (mcp's stateful two-message session push vs serve's one narrate line) stays entirely in the onStale callback. (5) `createShutdown({ channelLog, registration, closers }): () => Promise<void>` — owns the idempotence flag, channelLog.log("shutdown"), registration.remove(), awaits the caller-ordered closers, then channelLog.close() last; closers are raw twin-supplied thunks (e.g. `() => web.close().catch(() => {})`, `() => mcp.close().catch(() => {})`, stopWatch, unsubscribeTools, `() => recorder?.close()`) so each twin's exact ordering AND per-closer error-swallowing is preserved verbatim. (6) `installExitBackstop(registration)` — process.once("exit", () => registration.remove()). Signal wiring is deliberately NOT extracted (on vs once, SIGHUP, narration, mcp.onclose are semantic differences). program.ts: `parseBind(value)` throwing InvalidArgumentError + `addChannelServerOptions(cmd)` applying --bind/--mode with the shared strings at the point where the duplicated options are adjacent in both commands (43-59 / 84-99), preserving --help ordering. scripts/lib/common.mjs: `fail(message)`, `slugify(name)`, `currentVersion(scriptsDir)` (or computed-in-lib paths), `repoRoot`/`scriptsDir` constants, and `deriveContext()` adopting new-package's scanning implementation as the canonical one.

**Per-twin residue:** mcp.ts keeps: McpOptions extras (launchInfo, pageToolsNotify), launch-info parsing (85-92), the MCP server + stdio connect + pushToSession (106-133), the page-tools notification ladder (152-168), late-bound web thunk for channel_reload (103-114), its onStale callback with the notified toggle, SIGINT/SIGTERM/SIGHUP process.on wiring + mcp.onclose, its up-line fields (bind/ppid/cwd), and the startup push (270). serve.ts keeps: ServeOptions extras (name, record, cacheDir, port), parsePort + EADDRINUSE rewrap try/catch (140-146, 372-387), the recorder, the frame narrator + trace-stage narration + helloNarration (149-297, 330-333, 357-367), the stdout prompt/page-tools/ready-line protocol (340-347, 398-431, 482-484), registration extras {debug, name}, process.once SIGINT/SIGTERM with narration, and the ServeHandle return. program.ts keeps per-command --tag descriptions, quick's options, and serve's --port re-wrap. new-package.mjs keeps: LEVELS + its parseArgs (mandatory level, --no-reserve), skeleton copy/token walk, the --no-publish package.json strip, and npm-provision reserve flow. new-demo.ts keeps: its parseArgs (--help), scaffoldApp/templateRoot import, rewirePackageJson, TSCONFIG literal, the CLAUDE.md anchor-splice, readmeMd, title rewrite, and dotfile removal.

**Risks:** 1) Shutdown parity is the sharpest edge: which closes are .catch-swallowed differs per twin (mcp swallows web+mcp close; serve swallows only web.close), and channelLog.close() must stay last — the closers-as-raw-thunks design exists precisely so the helper never normalizes this; a 'cleaner' unified error policy would be a behavior change. 2) Signal semantics must not be unified: serve's process.once is documented as deliberate (serve.ts:470-471, second Ctrl-C escape hatch), mcp handles SIGHUP and stdio-close; extracting a shared wireSignals invites silently flattening these. 3) Test asymmetry: runServe is well covered (src/commands/serve.test.ts — lifecycle, registry, port collision, recording, sidecars), runMcp has NO direct test (stdio-driven); any lifecycle.ts refactor of mcp.ts needs a live `aiui claude` verification pass, not just the suite. 4) registerServer must remain after the web server binds in both twins (port is its argument); a tempting 'startChannelServer' mega-helper would also swallow serve's EADDRINUSE rewrap — keep the seam fine-grained. 5) program.ts option ordering feeds --help output and program.test.ts; apply addChannelServerOptions exactly where the duplicated adjacent options sit. 6) scripts/lib/common.mjs must stay plain ESM — one stray TS annotation breaks `pnpm new-package` under plain node; conversely converting new-package to tsx would contradict the documented dialect rationale (CLAUDE.md, new-demo.ts:11-14). 7) deriveContext unification (scan vs hardcoded packages/aiui) is output-identical in the current tree but is technically a behavior generalization for new-demo — if strict preservation is required, share only the return shape and keep both bodies. 8) The shared CommonChannelOptions JSDoc merges two slightly different doc voices (e.g. serve's 'natural place to exercise prod' note) — doc nuance loss only, but PARITY-style contract text should be checked. Effort/risk overall: pair (b) is low-risk (pure helpers, both scripts smoke-testable by scaffolding into a temp dir); pair (a) is medium — mechanical but touching the process-lifecycle of the one component whose failures are invisible (MCP subprocess stderr).

**Effort:** Pair (b): ~1-2 hours (extract scripts/lib/common.mjs, smoke-test both scaffolders end-to-end). Pair (a): ~0.5-1 day — lifecycle.ts extraction is mechanical (~200 duplicated lines collapse to ~6 helpers) but needs serve.test.ts green plus a manual live `aiui claude` session verify for the untested runMcp path; program.ts option dedup ~30 min. Total: about one focused day, done as two independent commits (scripts first — zero coupling to the channel work).

- [ ] Approve as proposed
- [ ] Partially (see comments)
- [ ] Defer
- [ ] Drop

Comments:
