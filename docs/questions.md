How to initiate the developer workflow? pnpm ai-pair
What are components:
  clauade code
  custom claude code channel
  vite dev server
  main app -- DUT
  browser dev tools (is this a chrome extension or  devtools panel or web overlay that runs in the browser? )
how to maintain state fo the dev tools across reloads?  (HMR, local storage, indexeddb, etc)
where does data live


Create a project spec