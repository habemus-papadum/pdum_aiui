{
  "name": "py-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture project for the aiui code reader. Holds the JS/TS + pyright language servers as project-local devDependencies so the committed, portable .aiui/lsp/ launchers resolve their servers from this project's own node_modules — a clone + install yields a working reader with no `aiui setup-lsp` step.",
  "devDependencies": {
    "pyright": "^1.1.411",
    "typescript": "^5.9.0",
    "typescript-language-server": "^5.3.0"
  }
}
