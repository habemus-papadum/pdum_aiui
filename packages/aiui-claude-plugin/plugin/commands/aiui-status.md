---
description: Report the current status of the aiui plugin (placeholder).
---

Print a short status summary for the aiui plugin.

This is a placeholder command — for now, run the bundled hello script and relay
its output:

```sh
node "${CLAUDE_PLUGIN_ROOT}/scripts/hello.mjs"
```

Then confirm to the user that the aiui plugin is installed and reachable.
