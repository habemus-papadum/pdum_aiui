/**
 * ai ui frontends
 *
 * The `aiui` package is a CLI, consumed via its bin; this barrel is a
 * placeholder surface. CLI internals live under `src/commands/` and
 * `src/util/` and are imported relatively.
 *
 * @packageDocumentation
 */

/** The published package name — handy for smoke tests. */
export const name = "@habemus-papadum/aiui";
